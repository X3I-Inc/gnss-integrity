# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg

# Virtual environments
venv/
.venv/
.venv_test/
env/
ENV/

# Testing / tooling
.pytest_cache/
.ruff_cache/
.mypy_cache/
.coverage
htmlcov/

# IDE
.vscode/
.idea/

# Jupyter
.ipynb_checkpoints/

# Project-specific: raw datasets and logs shouldn't be committed
data/raw/
*.log

# Raw TEXBAT recordings (large binaries). The small derived features.csv
# under data/texbat/<scenario>/ are kept in-tree so the detectors are
# reproducible without re-downloading the raw .mat files.
data/texbat/**/*.mat

# Vendored / unrelated sub-project checked out inside the working tree
TinyMLGNSS/

# Generated analysis plots written to the repo root by the *validate /
# plot_* scripts (regenerable; not source)
/*.png
